# KNOWN RESET-VALUE DEVIATIONS -- every line is a register where this model tells
# the guest something the silicon would never say.
#
#   ⚠ THIS NUMBER MUST GO DOWN.  It is a TO-DO LIST, NOT A CERTIFICATE.
#
# The gate fails in BOTH directions (mcxn947qemu / ollama_95_neutron):
#   * a mismatch NOT listed here      -> FAIL (a new lie)
#   * a listed entry that now MATCHES -> FAIL ("this is fixed -- delete the line")
# because "an independent whitelist does not merely MISS the bug, IT CERTIFIES IT",
# and an allowlist that can never shrink has stopped being a to-do list.
#
# THE TWO CLASSES ARE NOT EQUALLY BAD, and the reason column says which:
#   "block not modelled"  -- the catch-all answers 0. Honest-ish: nothing pretends
#                            to implement it, and the guest gets 0 for a register
#                            that does not exist here.  Still a lie, lower priority.
#   "MODELLED BLOCK"      -- WE implement this peripheral and got its reset wrong.
#                            The guest reads a plausible value from a block that
#                            claims to work.  THESE ARE THE DANGEROUS ONES.
#
ANADIG_OSC                 OSC_24M_CTRL           # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
ANADIG_PLL                 ARM_PLL_CTRL           # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
ANADIG_PLL                 PLL_AUDIO_CTRL         # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
ANADIG_PLL                 SYS_PLL1_CTRL          # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
ANADIG_PLL                 SYS_PLL2_CTRL          # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
ANADIG_PLL                 SYS_PLL3_CTRL          # STABLE/lock bit synthesised: we model the POST-BOOT-ROM state (our -kernel path skips the ROM, and firmware polls STABLE before asserting POWERUP)
AON_MIF_LN28FDSOI_SPLLRAM  MIF_MLPL_IG            # block not modelled; the catch-all answers 0
AON_MIF_LN28FDSOI_SPLLRAM  MIF_MLPL_PD_B          # block not modelled; the catch-all answers 0
AON_MIF_LN28FDSOI_SPLLRAM  MIF_MLPL_WLPD          # block not modelled; the catch-all answers 0
AON_MIX_SLICE              EDGELOCK_HDSK_CTRL     # block not modelled; the catch-all answers 0
AON_MIX_SLICE              EDGELOCK_HDSK_STAT     # block not modelled; the catch-all answers 0
AON_MIX_SLICE              FUNC_STAT              # block not modelled; the catch-all answers 0
AON_MIX_SLICE              LPM_SETTING_0          # block not modelled; the catch-all answers 0
AON_MIX_SLICE              LPM_SETTING_1          # block not modelled; the catch-all answers 0
AON_MIX_SLICE              LPM_SETTING_2          # block not modelled; the catch-all answers 0
AON_MIX_SLICE              MLPL_CFG               # block not modelled; the catch-all answers 0
AON_MIX_SLICE              MLPL_STAT              # block not modelled; the catch-all answers 0
AON_MIX_SLICE              PSW_CNT_CFG_HF         # block not modelled; the catch-all answers 0
AON_MIX_SLICE              PSW_CNT_CFG_LF         # block not modelled; the catch-all answers 0
AON_MIX_SLICE              PSW_CTRL               # block not modelled; the catch-all answers 0
ASRC                       ASR56K                 # block not modelled; the catch-all answers 0
ASRC                       ASR76K                 # block not modelled; the catch-all answers 0
BBNSM                      BBNSM_CTRL             # block not modelled; the catch-all answers 0
BBNSM                      BBNSM_EVENTS           # block not modelled; the catch-all answers 0
BBNSM                      BBNSM_FEATURES         # block not modelled; the catch-all answers 0
BBNSM                      BBNSM_INT_EN           # block not modelled; the catch-all answers 0
BBNSM                      BBNSM_VID              # block not modelled; the catch-all answers 0
BLK_CTRL_NS_AONMIX         SSI                    # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          AXBS_AON_CTRL          # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          EDGELOCK_IRQ_MASK      # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          EDGELOCK_RESET_REQ_MASK # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          IOMUXC_AON_DOMAIN_CFG  # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          IOMUXC_DOMAIN_CFG      # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          M33_CFG                # block not modelled; the catch-all answers 0
BLK_CTRL_S_AONMIX          M7_CFG                 # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         AXI_ATTR_CFG           # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         ECAT_MISC_CFG          # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         EMC_B1_IO_CTRL         # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         EMC_B2_IO_CTRL         # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         GPIO_B1_IO_CTRL        # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         GPIO_B2_IO_CTRL        # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         SD_B1_IO_CTRL          # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         SD_B2_IO_CTRL          # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         SSI                    # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         USBPHY_MISC_CTRL       # block not modelled; the catch-all answers 0
BLK_CTRL_WAKEUPMIX         XBAR_AOI_WE            # block not modelled; the catch-all answers 0
CAN1                       CTRL2                  # block not modelled; the catch-all answers 0
CAN1                       FDCTRL                 # block not modelled; the catch-all answers 0
CAN1                       MECR                   # block not modelled; the catch-all answers 0
CAN2                       CTRL2                  # block not modelled; the catch-all answers 0
CAN2                       FDCTRL                 # block not modelled; the catch-all answers 0
CAN2                       MECR                   # block not modelled; the catch-all answers 0
CAN3                       CTRL2                  # block not modelled; the catch-all answers 0
CAN3                       FDCTRL                 # block not modelled; the catch-all answers 0
CAN3                       MECR                   # block not modelled; the catch-all answers 0
CCM                        GPR_SHARED0_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED0_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED0_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED0_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED10_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED10_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED10_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED10_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED11_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED11_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED11_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED11_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED12_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED12_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED12_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED12_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED13_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED13_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED13_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED13_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED14_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED14_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED14_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED14_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED15_AUTHEN    # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED15_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED15_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED15_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED1_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED1_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED1_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED1_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED2_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED2_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED2_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED2_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED3_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED3_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED3_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED3_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED4_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED4_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED4_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED4_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED5_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED5_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED5_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED5_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED6_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED6_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED6_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED6_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED7_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED7_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED7_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED7_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED8_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED8_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED8_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED8_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED9_AUTHEN     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED9_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED9_AUTHEN_SET # MODELLED BLOCK, reset value not set -- a real lie, fix me
CCM                        GPR_SHARED9_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- a real lie, fix me
CM33PLATFORM_MIX_SLICE     EDGELOCK_HDSK_CTRL     # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     EDGELOCK_HDSK_STAT     # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     FUNC_STAT              # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     LPM_SETTING_0          # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     LPM_SETTING_1          # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     LPM_SETTING_2          # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     MLPL_CFG               # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     MLPL_STAT              # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     PSW_CNT_CFG_HF         # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     PSW_CNT_CFG_LF         # block not modelled; the catch-all answers 0
CM33PLATFORM_MIX_SLICE     PSW_CTRL               # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      EDGELOCK_HDSK_CTRL     # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      EDGELOCK_HDSK_STAT     # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      FUNC_STAT              # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      LPM_SETTING_0          # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      LPM_SETTING_1          # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      LPM_SETTING_2          # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      MLPL_CFG               # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      MLPL_STAT              # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      PSW_CNT_CFG_HF         # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      PSW_CNT_CFG_LF         # block not modelled; the catch-all answers 0
CM7PLATFORM_MIX_SLICE      PSW_CTRL               # block not modelled; the catch-all answers 0
CM7PLATFORM_TCM            MIF_DLY_ARR_HF         # block not modelled; the catch-all answers 0
CM7PLATFORM_TCM            MIF_DLY_SLEEP          # block not modelled; the catch-all answers 0
CM7PLATFORM_TCM            MIF_MLPL_ARR_PDN       # block not modelled; the catch-all answers 0
CM7PLATFORM_TCM            MIF_MLPL_SLEEP         # block not modelled; the catch-all answers 0
CMP1                       C3                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CMP2                       C3                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CMP3                       C3                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
CMP4                       C3                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
DAC                        CR                     # block not modelled; the catch-all answers 0
DCDC                       CURRENT_TRG            # block not modelled; the catch-all answers 0
DCDC                       FILTER_CNT             # block not modelled; the catch-all answers 0
DCDC                       OK_CNT                 # block not modelled; the catch-all answers 0
DCDC                       REG0                   # block not modelled; the catch-all answers 0
DCDC                       REG1                   # block not modelled; the catch-all answers 0
DCDC                       REG2                   # block not modelled; the catch-all answers 0
DCDC                       REG3                   # block not modelled; the catch-all answers 0
DCDC                       TRG_0_AUTHEN           # block not modelled; the catch-all answers 0
DCDC                       TRG_1_AUTHEN           # block not modelled; the catch-all answers 0
DCDC                       TRG_GPC_0              # block not modelled; the catch-all answers 0
DCDC                       TRG_GPC_1              # block not modelled; the catch-all answers 0
DCDC                       TRG_SW_0               # block not modelled; the catch-all answers 0
DCDC                       TRG_SW_1               # block not modelled; the catch-all answers 0
ECAT                       AL_EVENT_REQUEST       # block not modelled; the catch-all answers 0
ECAT                       ESC_DL_CONTROL         # block not modelled; the catch-all answers 0
ECAT                       PDI_AL_EVENT_MASK      # block not modelled; the catch-all answers 0
ECAT                       RECEIVE_TIMES          # block not modelled; the catch-all answers 0
ECAT                       RECEIVE_TIME_PORT_1    # block not modelled; the catch-all answers 0
EMDIO_BASE                 EMDIO_CFG              # block not modelled; the catch-all answers 0
EMDIO_BASE                 MDIO_CFG               # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               HBTCAPR                # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               HBTOR2                 # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               IPBRR0                 # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               IPBRR1                 # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               NETCCLKR               # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               SMACR                  # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               SMBUCR                 # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               SMCAPR                 # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               SMCLWMR                # block not modelled; the catch-all answers 0
EMDIO_GLOBAL               SMDTR                  # block not modelled; the catch-all answers 0
ENETC0_BASE                PMR                    # block not modelled; the catch-all answers 0
ENETC0_BASE                PRIO2TCMR0             # block not modelled; the catch-all answers 0
ENETC0_COMMON              ECTCAPR                # block not modelled; the catch-all answers 0
ENETC0_COMMON              ESQRCAPR               # block not modelled; the catch-all answers 0
ENETC0_COMMON              ESQRTCAPR              # block not modelled; the catch-all answers 0
ENETC0_COMMON              ETCAPR                 # block not modelled; the catch-all answers 0
ENETC0_COMMON              ETTCAPR                # block not modelled; the catch-all answers 0
ENETC0_COMMON              FMECAPR                # block not modelled; the catch-all answers 0
ENETC0_COMMON              FMICAPR                # block not modelled; the catch-all answers 0
ENETC0_COMMON              FMITCAPR               # block not modelled; the catch-all answers 0
ENETC0_COMMON              ISFHTCAPR              # block not modelled; the catch-all answers 0
ENETC0_COMMON              OSR                    # block not modelled; the catch-all answers 0
ENETC0_COMMON              RPCAPR                 # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PEMDIOSR               # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM0_IEVENT             # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM0_IF_MODE            # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM0_MAXFRM             # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM0_MDIO_CFG           # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM0_MINFRM             # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM1_IEVENT             # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM1_IF_MODE            # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM1_MAXFRM             # block not modelled; the catch-all answers 0
ENETC0_ETH_MAC_PORT        PM1_MINFRM             # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              HBTCAPR                # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              HBTOR2                 # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              IPBRR0                 # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              IPBRR1                 # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              NETCCLKR               # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              SMACR                  # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              SMBUCR                 # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              SMCAPR                 # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              SMCLWMR                # block not modelled; the catch-all answers 0
ENETC0_GLOBAL              SMDTR                  # block not modelled; the catch-all answers 0
ENETC0_PORT                BPDVR                  # block not modelled; the catch-all answers 0
ENETC0_PORT                BPSCR0                 # block not modelled; the catch-all answers 0
ENETC0_PORT                BPSCR1                 # block not modelled; the catch-all answers 0
ENETC0_PORT                BPSTGSR                # block not modelled; the catch-all answers 0
ENETC0_PORT                PCR                    # block not modelled; the catch-all answers 0
ENETC0_PORT                PCTFCR                 # block not modelled; the catch-all answers 0
ENETC0_PORT                PDGSR                  # block not modelled; the catch-all answers 0
ENETC0_PORT                PIPV2QMR0              # block not modelled; the catch-all answers 0
ENETC0_PORT                PISIDCR                # block not modelled; the catch-all answers 0
ENETC0_PORT                PPCR                   # block not modelled; the catch-all answers 0
ENETC0_PORT                PRXSDUOR               # block not modelled; the catch-all answers 0
ENETC0_PORT                PTAR                   # block not modelled; the catch-all answers 0
ENETC0_PORT                PTGSHCR                # block not modelled; the catch-all answers 0
ENETC0_PORT                PTXSDUOR               # block not modelled; the catch-all answers 0
ENETC0_SI0                 SIBCAR                 # block not modelled; the catch-all answers 0
ENETC0_SI0                 SICAPR1                # block not modelled; the catch-all answers 0
ENETC0_SI0                 SICCAR                 # block not modelled; the catch-all answers 0
ENETC0_SI0                 SIMCAR                 # block not modelled; the catch-all answers 0
ENETC0_SI0                 SISR                   # block not modelled; the catch-all answers 0
ENETC1_BASE                PMR                    # block not modelled; the catch-all answers 0
ENETC1_BASE                PRIO2TCMR0             # block not modelled; the catch-all answers 0
ENETC1_COMMON              ECTCAPR                # block not modelled; the catch-all answers 0
ENETC1_COMMON              ESQRCAPR               # block not modelled; the catch-all answers 0
ENETC1_COMMON              ESQRTCAPR              # block not modelled; the catch-all answers 0
ENETC1_COMMON              ETCAPR                 # block not modelled; the catch-all answers 0
ENETC1_COMMON              ETTCAPR                # block not modelled; the catch-all answers 0
ENETC1_COMMON              FMECAPR                # block not modelled; the catch-all answers 0
ENETC1_COMMON              FMICAPR                # block not modelled; the catch-all answers 0
ENETC1_COMMON              FMITCAPR               # block not modelled; the catch-all answers 0
ENETC1_COMMON              ISFHTCAPR              # block not modelled; the catch-all answers 0
ENETC1_COMMON              OSR                    # block not modelled; the catch-all answers 0
ENETC1_COMMON              RPCAPR                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              HBTCAPR                # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              HBTOR2                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              IPBRR0                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              IPBRR1                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              NETCCLKR               # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              SMACR                  # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              SMBUCR                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              SMCAPR                 # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              SMCLWMR                # block not modelled; the catch-all answers 0
ENETC1_GLOBAL              SMDTR                  # block not modelled; the catch-all answers 0
ENETC1_PORT                BPDVR                  # block not modelled; the catch-all answers 0
ENETC1_PORT                BPSCR0                 # block not modelled; the catch-all answers 0
ENETC1_PORT                BPSCR1                 # block not modelled; the catch-all answers 0
ENETC1_PORT                BPSTGSR                # block not modelled; the catch-all answers 0
ENETC1_PORT                PCR                    # block not modelled; the catch-all answers 0
ENETC1_PORT                PCTFCR                 # block not modelled; the catch-all answers 0
ENETC1_PORT                PDGSR                  # block not modelled; the catch-all answers 0
ENETC1_PORT                PIPV2QMR0              # block not modelled; the catch-all answers 0
ENETC1_PORT                PISIDCR                # block not modelled; the catch-all answers 0
ENETC1_PORT                PPCR                   # block not modelled; the catch-all answers 0
ENETC1_PORT                PRXSDUOR               # block not modelled; the catch-all answers 0
ENETC1_PORT                PTAR                   # block not modelled; the catch-all answers 0
ENETC1_PORT                PTGSHCR                # block not modelled; the catch-all answers 0
ENETC1_PORT                PTXSDUOR               # block not modelled; the catch-all answers 0
ENETC1_PSEUDO_MAC_PORT     PPMCR                  # block not modelled; the catch-all answers 0
ENETC1_PSEUDO_MAC_PORT     PPMSR                  # block not modelled; the catch-all answers 0
ENETC1_SI0                 SIBCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI0                 SICAPR1                # block not modelled; the catch-all answers 0
ENETC1_SI0                 SICCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI0                 SIMCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI0                 SISR                   # block not modelled; the catch-all answers 0
ENETC1_SI1                 SIBCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI1                 SICAPR1                # block not modelled; the catch-all answers 0
ENETC1_SI1                 SICCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI1                 SIMCAR                 # block not modelled; the catch-all answers 0
ENETC1_SI1                 SISR                   # block not modelled; the catch-all answers 0
FLEXSPI1                   AHBCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   FLSHCR4                # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   INTR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   LUTCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   LUTKEY                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   MCR0                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   MCR1                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   MCR2                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   STS0                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI1                   STS2                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   AHBCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   FLSHCR4                # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   INTR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   LUTCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   LUTKEY                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   MCR0                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   MCR1                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   MCR2                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   STS0                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI2                   STS2                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI_SLV                MODULE_CONTROL         # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI_SLV                MODULE_STATUS          # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI_SLV                READ_REGISTER_COMMAND0 # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI_SLV                RW_COMMAND_BASE        # MODELLED BLOCK, reset value not set -- a real lie, fix me
FLEXSPI_SLV                WRITE_COMMAND_CONTROL  # MODELLED BLOCK, reset value not set -- a real lie, fix me
GPC_SYS_SLEEP_CTRL         PMIC_CTRL              # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_DCDC_IN_CTRL        # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_DCDC_OUT_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_PMIC_IN_CTRL        # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_PMIC_OUT_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP0_IN_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP0_OUT_CTRL      # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP1_IN_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP1_OUT_CTRL      # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP2_IN_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP3_IN_CTRL       # block not modelled; the catch-all answers 0
GPC_SYS_SLEEP_CTRL         SS_STEP3_OUT_CTRL      # block not modelled; the catch-all answers 0
I3C1                       IBIEXT1                # block not modelled; the catch-all answers 0
I3C1                       MDATACTRL              # block not modelled; the catch-all answers 0
I3C1                       MDMACTRL               # block not modelled; the catch-all answers 0
I3C1                       MSTATUS                # block not modelled; the catch-all answers 0
I3C1                       SCAPABILITIES          # block not modelled; the catch-all answers 0
I3C1                       SCAPABILITIES2         # block not modelled; the catch-all answers 0
I3C1                       SCCCMASK               # block not modelled; the catch-all answers 0
I3C1                       SDATACTRL              # block not modelled; the catch-all answers 0
I3C1                       SDMACTRL               # block not modelled; the catch-all answers 0
I3C1                       SERRWARNMASK           # block not modelled; the catch-all answers 0
I3C1                       SIDEXT                 # block not modelled; the catch-all answers 0
I3C1                       SIDPARTNO              # block not modelled; the catch-all answers 0
I3C1                       SINTSET                # block not modelled; the catch-all answers 0
I3C1                       SSTATUS                # block not modelled; the catch-all answers 0
I3C1                       STCCLOCK               # block not modelled; the catch-all answers 0
I3C1                       SVENDORID              # block not modelled; the catch-all answers 0
I3C2                       IBIEXT1                # block not modelled; the catch-all answers 0
I3C2                       MDATACTRL              # block not modelled; the catch-all answers 0
I3C2                       MDMACTRL               # block not modelled; the catch-all answers 0
I3C2                       MSTATUS                # block not modelled; the catch-all answers 0
I3C2                       SCAPABILITIES          # block not modelled; the catch-all answers 0
I3C2                       SCAPABILITIES2         # block not modelled; the catch-all answers 0
I3C2                       SCCCMASK               # block not modelled; the catch-all answers 0
I3C2                       SDATACTRL              # block not modelled; the catch-all answers 0
I3C2                       SDMACTRL               # block not modelled; the catch-all answers 0
I3C2                       SERRWARNMASK           # block not modelled; the catch-all answers 0
I3C2                       SIDEXT                 # block not modelled; the catch-all answers 0
I3C2                       SIDPARTNO              # block not modelled; the catch-all answers 0
I3C2                       SINTSET                # block not modelled; the catch-all answers 0
I3C2                       SSTATUS                # block not modelled; the catch-all answers 0
I3C2                       STCCLOCK               # block not modelled; the catch-all answers 0
I3C2                       SVENDORID              # block not modelled; the catch-all answers 0
LPI2C1                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C1                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C1                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C1                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C2                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C2                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C2                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C2                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C3                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C3                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C3                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C3                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C4                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C4                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C4                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C4                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     MRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     MRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C5                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     MRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     MRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     MSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     SASR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     SRDR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPI2C6                     SRDROR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI1                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI1                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI2                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI2                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI3                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI3                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI4                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI4                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI5                     RSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI5                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI5                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI6                     RSR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI6                     SR                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPSPI6                     TCR                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART1                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART1                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART1                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART1                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART1                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART10                   BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART10                   DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART10                   DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART10                   FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART10                   TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART11                   BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART11                   DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART11                   DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART11                   FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART11                   TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART12                   BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART12                   DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART12                   DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART12                   FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART12                   TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART2                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART2                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART2                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART2                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART2                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART3                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART3                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART3                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART3                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART3                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART4                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART4                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART4                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART4                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART4                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART5                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART5                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART5                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART5                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART5                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART6                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART6                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART6                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART6                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART6                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART7                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART7                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART7                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART7                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART7                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART8                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART8                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART8                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART8                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART8                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART9                    BAUD                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART9                    DATA                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART9                    DATARO                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART9                    FIFO                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
LPUART9                    TOSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
MEGA_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_IG            # block not modelled; the catch-all answers 0
MEGA_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_PD_B          # block not modelled; the catch-all answers 0
MEGA_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_WLPD          # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             EDGELOCK_HDSK_CTRL     # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             EDGELOCK_HDSK_STAT     # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             FUNC_STAT              # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             LPM_SETTING_0          # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             LPM_SETTING_1          # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             LPM_SETTING_2          # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             MLPL_CFG               # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             MLPL_STAT              # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             PSW_CNT_CFG_HF         # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             PSW_CNT_CFG_LF         # block not modelled; the catch-all answers 0
MEGA_MIX_SLICE             PSW_CTRL               # block not modelled; the catch-all answers 0
NETC_F0_PCI_HDR_TYPE0      PCIE_CFC_SRIOV_CAP_HDR # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_F1_PCI_HDR_TYPE0      PCIE_CFC_SRIOV_CAP_HDR # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_F2_PCI_HDR_TYPE0      PCIE_CFC_SRIOV_CAP_HDR # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_F3_PCI_HDR_TYPE0      PCIE_CFC_SRIOV_CAP_HDR # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_F4_PCI_HDR_TYPE0      PCIE_CFC_SRIOV_CAP_HDR # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  CAPR0                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  CAPR1                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  CAPR2                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  CAPR3                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  CMCAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  EMDIOBCR               # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  EMDIO_CFG              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  HBTCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  HBTMAR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  IPFTMCAPR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L0CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L0TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L1BCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L1CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L1TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L2BCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L2CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L2TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L3BCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L3CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L3TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L4BCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L4CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L4TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L5BCR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L5CAPR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  L5TXBCCTR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  NETCCLKCR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  NETCCLKFR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  NETCFLRCR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  SGLTTR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  SMDTR                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_IERB                  TGSMCAPR               # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_IG            # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_PD_B          # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_WLPD          # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             EDGELOCK_HDSK_CTRL     # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             EDGELOCK_HDSK_STAT     # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             FUNC_STAT              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             LPM_SETTING_0          # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             LPM_SETTING_1          # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             LPM_SETTING_2          # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             MLPL_CFG               # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             MLPL_STAT              # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             PSW_CNT_CFG_HF         # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             PSW_CNT_CFG_LF         # MODELLED BLOCK, reset value not set -- a real lie, fix me
NETC_MIX_SLICE             PSW_CTRL               # MODELLED BLOCK, reset value not set -- a real lie, fix me
OTFAD1                     SR                     # block not modelled; the catch-all answers 0
OTFAD2                     SR                     # block not modelled; the catch-all answers 0
PDM                        FIFO_CTRL              # block not modelled; the catch-all answers 0
PDM                        VAD0_CTRL_2            # block not modelled; the catch-all answers 0
PDM                        VAD0_NCONFIG           # block not modelled; the catch-all answers 0
PDM                        VAD0_STAT              # block not modelled; the catch-all answers 0
PDM                        VAD0_ZCD               # block not modelled; the catch-all answers 0
RTWDOG1                    CS                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG1                    TOVAL                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG2                    CS                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG2                    TOVAL                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG3                    CS                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG3                    TOVAL                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG4                    CS                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG4                    TOVAL                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG5                    CS                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
RTWDOG5                    TOVAL                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
SAI1                       TCSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
SAI2                       TCSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
SAI3                       TCSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
SAI4                       TCSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
SEMC                       BR10                   # block not modelled; the catch-all answers 0
SEMC                       BR11                   # block not modelled; the catch-all answers 0
SEMC                       BR9                    # block not modelled; the catch-all answers 0
SEMC                       DLLCR                  # block not modelled; the catch-all answers 0
SEMC                       NANDCR2                # block not modelled; the catch-all answers 0
SEMC                       SDRAMCR0               # block not modelled; the catch-all answers 0
SEMC                       SDRAMCR1               # block not modelled; the catch-all answers 0
SEMC                       SDRAMCR2               # block not modelled; the catch-all answers 0
SEMC                       SDRAMCR3               # block not modelled; the catch-all answers 0
SEMC                       STS0                   # block not modelled; the catch-all answers 0
SEMC                       STS13                  # block not modelled; the catch-all answers 0
SINC1                      PARAMETER              # block not modelled; the catch-all answers 0
SINC1                      SR                     # block not modelled; the catch-all answers 0
SINC2                      PARAMETER              # block not modelled; the catch-all answers 0
SINC2                      SR                     # block not modelled; the catch-all answers 0
SINC3                      PARAMETER              # block not modelled; the catch-all answers 0
SINC3                      SR                     # block not modelled; the catch-all answers 0
SPDIF                      SCR                    # block not modelled; the catch-all answers 0
SPDIF                      SIS                    # block not modelled; the catch-all answers 0
SPDIF                      STC                    # block not modelled; the catch-all answers 0
SRC_GENERAL_REG            SBMR2                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
SRC_GENERAL_REG            SRMASK                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
SRC_GENERAL_REG            SRSR                   # MODELLED BLOCK, reset value not set -- a real lie, fix me
SRC_GENERAL_REG            SRSR_BBSM              # MODELLED BLOCK, reset value not set -- a real lie, fix me
SW0_BASE                   BPCAPR                 # block not modelled; the catch-all answers 0
SW0_BASE                   BRCAPR                 # block not modelled; the catch-all answers 0
SW0_BASE                   CCAR                   # block not modelled; the catch-all answers 0
SW0_BASE                   FCAPR                  # block not modelled; the catch-all answers 0
SW0_BASE                   FDBHTCAPR              # block not modelled; the catch-all answers 0
SW0_BASE                   IMDCR0                 # block not modelled; the catch-all answers 0
SW0_BASE                   IPMFHTCAPR             # block not modelled; the catch-all answers 0
SW0_BASE                   MPCR                   # block not modelled; the catch-all answers 0
SW0_BASE                   SCAPR0                 # block not modelled; the catch-all answers 0
SW0_BASE                   SCAPR1                 # block not modelled; the catch-all answers 0
SW0_BASE                   SMBCAPR                # block not modelled; the catch-all answers 0
SW0_BASE                   VFHTCAPR               # block not modelled; the catch-all answers 0
SW0_COMMON                 ECTCAPR                # block not modelled; the catch-all answers 0
SW0_COMMON                 ESQRCAPR               # block not modelled; the catch-all answers 0
SW0_COMMON                 ESQRTCAPR              # block not modelled; the catch-all answers 0
SW0_COMMON                 ETCAPR                 # block not modelled; the catch-all answers 0
SW0_COMMON                 ETTCAPR                # block not modelled; the catch-all answers 0
SW0_COMMON                 FMECAPR                # block not modelled; the catch-all answers 0
SW0_COMMON                 FMICAPR                # block not modelled; the catch-all answers 0
SW0_COMMON                 FMITCAPR               # block not modelled; the catch-all answers 0
SW0_COMMON                 ISFHTCAPR              # block not modelled; the catch-all answers 0
SW0_COMMON                 OSR                    # block not modelled; the catch-all answers 0
SW0_COMMON                 RPCAPR                 # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PEMDIOSR               # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM0_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM0_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM0_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM0_MDIO_CFG           # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM0_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM1_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM1_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM1_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT0          PM1_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PEMDIOSR               # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM0_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM0_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM0_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM0_MDIO_CFG           # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM0_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM1_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM1_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM1_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT1          PM1_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PEMDIOSR               # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM0_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM0_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM0_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM0_MDIO_CFG           # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM0_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM1_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM1_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM1_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT2          PM1_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PEMDIOSR               # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM0_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM0_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM0_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM0_MDIO_CFG           # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM0_MINFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM1_IEVENT             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM1_IF_MODE            # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM1_MAXFRM             # block not modelled; the catch-all answers 0
SW0_ETH_MAC_PORT3          PM1_MINFRM             # block not modelled; the catch-all answers 0
SW0_GLOBAL                 HBTCAPR                # block not modelled; the catch-all answers 0
SW0_GLOBAL                 HBTOR2                 # block not modelled; the catch-all answers 0
SW0_GLOBAL                 IPBRR0                 # block not modelled; the catch-all answers 0
SW0_GLOBAL                 IPBRR1                 # block not modelled; the catch-all answers 0
SW0_GLOBAL                 NETCCLKR               # block not modelled; the catch-all answers 0
SW0_GLOBAL                 SMACR                  # block not modelled; the catch-all answers 0
SW0_GLOBAL                 SMBUCR                 # block not modelled; the catch-all answers 0
SW0_GLOBAL                 SMCAPR                 # block not modelled; the catch-all answers 0
SW0_GLOBAL                 SMCLWMR                # block not modelled; the catch-all answers 0
SW0_GLOBAL                 SMDTR                  # block not modelled; the catch-all answers 0
SW0_PORT0                  BPDVR                  # block not modelled; the catch-all answers 0
SW0_PORT0                  BPSCR0                 # block not modelled; the catch-all answers 0
SW0_PORT0                  BPSCR1                 # block not modelled; the catch-all answers 0
SW0_PORT0                  BPSTGSR                # block not modelled; the catch-all answers 0
SW0_PORT0                  PCR                    # block not modelled; the catch-all answers 0
SW0_PORT0                  PCTFCR                 # block not modelled; the catch-all answers 0
SW0_PORT0                  PDGSR                  # block not modelled; the catch-all answers 0
SW0_PORT0                  PIPV2QMR0              # block not modelled; the catch-all answers 0
SW0_PORT0                  PISIDCR                # block not modelled; the catch-all answers 0
SW0_PORT0                  PPCR                   # block not modelled; the catch-all answers 0
SW0_PORT0                  PRXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT0                  PTAR                   # block not modelled; the catch-all answers 0
SW0_PORT0                  PTGSHCR                # block not modelled; the catch-all answers 0
SW0_PORT0                  PTXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT1                  BPDVR                  # block not modelled; the catch-all answers 0
SW0_PORT1                  BPSCR0                 # block not modelled; the catch-all answers 0
SW0_PORT1                  BPSCR1                 # block not modelled; the catch-all answers 0
SW0_PORT1                  BPSTGSR                # block not modelled; the catch-all answers 0
SW0_PORT1                  PCR                    # block not modelled; the catch-all answers 0
SW0_PORT1                  PCTFCR                 # block not modelled; the catch-all answers 0
SW0_PORT1                  PDGSR                  # block not modelled; the catch-all answers 0
SW0_PORT1                  PIPV2QMR0              # block not modelled; the catch-all answers 0
SW0_PORT1                  PISIDCR                # block not modelled; the catch-all answers 0
SW0_PORT1                  PPCR                   # block not modelled; the catch-all answers 0
SW0_PORT1                  PRXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT1                  PTAR                   # block not modelled; the catch-all answers 0
SW0_PORT1                  PTGSHCR                # block not modelled; the catch-all answers 0
SW0_PORT1                  PTXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT2                  BPDVR                  # block not modelled; the catch-all answers 0
SW0_PORT2                  BPSCR0                 # block not modelled; the catch-all answers 0
SW0_PORT2                  BPSCR1                 # block not modelled; the catch-all answers 0
SW0_PORT2                  BPSTGSR                # block not modelled; the catch-all answers 0
SW0_PORT2                  PCR                    # block not modelled; the catch-all answers 0
SW0_PORT2                  PCTFCR                 # block not modelled; the catch-all answers 0
SW0_PORT2                  PDGSR                  # block not modelled; the catch-all answers 0
SW0_PORT2                  PIPV2QMR0              # block not modelled; the catch-all answers 0
SW0_PORT2                  PISIDCR                # block not modelled; the catch-all answers 0
SW0_PORT2                  PPCR                   # block not modelled; the catch-all answers 0
SW0_PORT2                  PRXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT2                  PTAR                   # block not modelled; the catch-all answers 0
SW0_PORT2                  PTGSHCR                # block not modelled; the catch-all answers 0
SW0_PORT2                  PTXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT3                  BPDVR                  # block not modelled; the catch-all answers 0
SW0_PORT3                  BPSCR0                 # block not modelled; the catch-all answers 0
SW0_PORT3                  BPSCR1                 # block not modelled; the catch-all answers 0
SW0_PORT3                  BPSTGSR                # block not modelled; the catch-all answers 0
SW0_PORT3                  PCR                    # block not modelled; the catch-all answers 0
SW0_PORT3                  PCTFCR                 # block not modelled; the catch-all answers 0
SW0_PORT3                  PDGSR                  # block not modelled; the catch-all answers 0
SW0_PORT3                  PIPV2QMR0              # block not modelled; the catch-all answers 0
SW0_PORT3                  PISIDCR                # block not modelled; the catch-all answers 0
SW0_PORT3                  PPCR                   # block not modelled; the catch-all answers 0
SW0_PORT3                  PRXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT3                  PTAR                   # block not modelled; the catch-all answers 0
SW0_PORT3                  PTGSHCR                # block not modelled; the catch-all answers 0
SW0_PORT3                  PTXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT4                  BPDVR                  # block not modelled; the catch-all answers 0
SW0_PORT4                  BPSCR0                 # block not modelled; the catch-all answers 0
SW0_PORT4                  BPSCR1                 # block not modelled; the catch-all answers 0
SW0_PORT4                  BPSTGSR                # block not modelled; the catch-all answers 0
SW0_PORT4                  PCR                    # block not modelled; the catch-all answers 0
SW0_PORT4                  PCTFCR                 # block not modelled; the catch-all answers 0
SW0_PORT4                  PDGSR                  # block not modelled; the catch-all answers 0
SW0_PORT4                  PIPV2QMR0              # block not modelled; the catch-all answers 0
SW0_PORT4                  PISIDCR                # block not modelled; the catch-all answers 0
SW0_PORT4                  PPCR                   # block not modelled; the catch-all answers 0
SW0_PORT4                  PRXSDUOR               # block not modelled; the catch-all answers 0
SW0_PORT4                  PTAR                   # block not modelled; the catch-all answers 0
SW0_PORT4                  PTGSHCR                # block not modelled; the catch-all answers 0
SW0_PORT4                  PTXSDUOR               # block not modelled; the catch-all answers 0
SW0_PSEUDO_MAC_PORT4       PPMCR                  # block not modelled; the catch-all answers 0
SW0_PSEUDO_MAC_PORT4       PPMSR                  # block not modelled; the catch-all answers 0
SYS_CTR_CONTROL            CNTFID0                # block not modelled; the catch-all answers 0
SYS_CTR_CONTROL            CNTFID1                # block not modelled; the catch-all answers 0
SYS_CTR_CONTROL            CNTSR                  # block not modelled; the catch-all answers 0
TMPSNS                     STATUS0                # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_CAPR               # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_CTRL               # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_FIPER_CTRL         # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_ID                 # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_PRSC               # block not modelled; the catch-all answers 0
TMR0_BASE                  TMR_STAT               # block not modelled; the catch-all answers 0
TMR0_GLOBAL                HBTCAPR                # block not modelled; the catch-all answers 0
TMR0_GLOBAL                HBTOR2                 # block not modelled; the catch-all answers 0
TMR0_GLOBAL                IPBRR0                 # block not modelled; the catch-all answers 0
TMR0_GLOBAL                IPBRR1                 # block not modelled; the catch-all answers 0
TMR0_GLOBAL                NETCCLKR               # block not modelled; the catch-all answers 0
TMR0_GLOBAL                SMACR                  # block not modelled; the catch-all answers 0
TMR0_GLOBAL                SMBUCR                 # block not modelled; the catch-all answers 0
TMR0_GLOBAL                SMCAPR                 # block not modelled; the catch-all answers 0
TMR0_GLOBAL                SMCLWMR                # block not modelled; the catch-all answers 0
TMR0_GLOBAL                SMDTR                  # block not modelled; the catch-all answers 0
TPM1                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TPM2                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TPM3                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TPM4                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TPM5                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TPM6                       MOD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC1                      TRDC_CR                # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC1                      TRDC_FLW_PBASE         # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC1                      TRDC_IDAU_CR           # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC2                      TRDC_CR                # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC2                      TRDC_FLW_PBASE         # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC2                      TRDC_IDAU_CR           # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC3                      TRDC_CR                # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC3                      TRDC_FLW_PBASE         # MODELLED BLOCK, reset value not set -- a real lie, fix me
TRDC3                      TRDC_IDAU_CR           # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBHSDCD1                  CLOCK                  # block not modelled; the catch-all answers 0
USBHSDCD1                  TIMER0                 # block not modelled; the catch-all answers 0
USBHSDCD1                  TIMER1                 # block not modelled; the catch-all answers 0
USBHSDCD1                  TIMER2_BC11            # block not modelled; the catch-all answers 0
USBHSDCD2                  CLOCK                  # block not modelled; the catch-all answers 0
USBHSDCD2                  TIMER0                 # block not modelled; the catch-all answers 0
USBHSDCD2                  TIMER1                 # block not modelled; the catch-all answers 0
USBHSDCD2                  TIMER2_BC11            # block not modelled; the catch-all answers 0
USBNC_OTG1                 CTRL1                  # block not modelled; the catch-all answers 0
USBNC_OTG1                 HSIC_CTRL              # block not modelled; the catch-all answers 0
USBNC_OTG2                 CTRL1                  # block not modelled; the catch-all answers 0
USBNC_OTG2                 HSIC_CTRL              # block not modelled; the catch-all answers 0
USBPHY1                    ANACTRL                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    ANACTRL_CLR            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    ANACTRL_SET            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    ANACTRL_TOG            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG1                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG1_CLR             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG1_SET             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG1_TOG             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG_CLR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG_SET              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    DEBUG_TOG              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PLL_SIC                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PLL_SIC_CLR            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PLL_SIC_SET            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PLL_SIC_TOG            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PWD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PWD_CLR                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PWD_SET                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    PWD_TOG                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TRIM_OVERRIDE_EN       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TRIM_OVERRIDE_EN_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TRIM_OVERRIDE_EN_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TRIM_OVERRIDE_EN_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TX                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TX_CLR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TX_SET                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    TX_TOG                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_CHRG_DETECT       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_CHRG_DETECT_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_CHRG_DETECT_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_CHRG_DETECT_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_LOOPBACK          # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_LOOPBACK_CLR      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_LOOPBACK_SET      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_LOOPBACK_TOG      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_VBUS_DETECT       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_VBUS_DETECT_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_VBUS_DETECT_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_VBUS_DETECT_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    USB1_VBUS_DET_STAT     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY1                    VERSION                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    ANACTRL                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    ANACTRL_CLR            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    ANACTRL_SET            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    ANACTRL_TOG            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG1                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG1_CLR             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG1_SET             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG1_TOG             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG_CLR              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG_SET              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    DEBUG_TOG              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PLL_SIC                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PLL_SIC_CLR            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PLL_SIC_SET            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PLL_SIC_TOG            # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PWD                    # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PWD_CLR                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PWD_SET                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    PWD_TOG                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TRIM_OVERRIDE_EN       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TRIM_OVERRIDE_EN_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TRIM_OVERRIDE_EN_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TRIM_OVERRIDE_EN_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TX                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TX_CLR                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TX_SET                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    TX_TOG                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_CHRG_DETECT       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_CHRG_DETECT_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_CHRG_DETECT_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_CHRG_DETECT_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_LOOPBACK          # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_LOOPBACK_CLR      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_LOOPBACK_SET      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_LOOPBACK_TOG      # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_VBUS_DETECT       # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_VBUS_DETECT_CLR   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_VBUS_DETECT_SET   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_VBUS_DETECT_TOG   # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    USB1_VBUS_DET_STAT     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USBPHY2                    VERSION                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   BURSTSIZE              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   CONFIGFLAG             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   ENDPTCTRL0             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HCCPARAMS              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HCSPARAMS              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HWDEVICE               # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HWGENERAL              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HWHOST                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HWRXBUF                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   HWTXBUF                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   ID                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   OTGSC                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   PORTSC1                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   SBUSCFG                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   USBCMD                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   USBMODE                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG1                   USBSTS                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   BURSTSIZE              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   CONFIGFLAG             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   ENDPTCTRL0             # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HCCPARAMS              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HCSPARAMS              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HWDEVICE               # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HWGENERAL              # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HWHOST                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HWRXBUF                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   HWTXBUF                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   ID                     # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   OTGSC                  # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   PORTSC1                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   SBUSCFG                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   USBCMD                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   USBMODE                # MODELLED BLOCK, reset value not set -- a real lie, fix me
USB_OTG2                   USBSTS                 # MODELLED BLOCK, reset value not set -- a real lie, fix me
USDHC1                     BLK_ATT                # block not modelled; the catch-all answers 0
USDHC1                     CQCAP                  # block not modelled; the catch-all answers 0
USDHC1                     CQRMEM                 # block not modelled; the catch-all answers 0
USDHC1                     CQSSC1                 # block not modelled; the catch-all answers 0
USDHC1                     CQVER                  # block not modelled; the catch-all answers 0
USDHC1                     DLL_STATUS             # block not modelled; the catch-all answers 0
USDHC1                     HOST_CTRL_CAP          # block not modelled; the catch-all answers 0
USDHC1                     MIX_CTRL               # block not modelled; the catch-all answers 0
USDHC1                     PROT_CTRL              # block not modelled; the catch-all answers 0
USDHC1                     STROBE_DLL_STATUS      # block not modelled; the catch-all answers 0
USDHC1                     SYS_CTRL               # block not modelled; the catch-all answers 0
USDHC1                     TUNING_CTRL            # block not modelled; the catch-all answers 0
USDHC1                     VEND_SPEC              # block not modelled; the catch-all answers 0
USDHC1                     VEND_SPEC2             # block not modelled; the catch-all answers 0
USDHC1                     WTMK_LVL               # block not modelled; the catch-all answers 0
USDHC2                     BLK_ATT                # block not modelled; the catch-all answers 0
USDHC2                     CQCAP                  # block not modelled; the catch-all answers 0
USDHC2                     CQRMEM                 # block not modelled; the catch-all answers 0
USDHC2                     CQSSC1                 # block not modelled; the catch-all answers 0
USDHC2                     CQVER                  # block not modelled; the catch-all answers 0
USDHC2                     DLL_STATUS             # block not modelled; the catch-all answers 0
USDHC2                     HOST_CTRL_CAP          # block not modelled; the catch-all answers 0
USDHC2                     MIX_CTRL               # block not modelled; the catch-all answers 0
USDHC2                     PROT_CTRL              # block not modelled; the catch-all answers 0
USDHC2                     STROBE_DLL_STATUS      # block not modelled; the catch-all answers 0
USDHC2                     SYS_CTRL               # block not modelled; the catch-all answers 0
USDHC2                     TUNING_CTRL            # block not modelled; the catch-all answers 0
USDHC2                     VEND_SPEC              # block not modelled; the catch-all answers 0
USDHC2                     VEND_SPEC2             # block not modelled; the catch-all answers 0
USDHC2                     WTMK_LVL               # block not modelled; the catch-all answers 0
WAKEUP_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_IG            # block not modelled; the catch-all answers 0
WAKEUP_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_PD_B          # block not modelled; the catch-all answers 0
WAKEUP_MIF_LN28FDSOI_SPLLRAM MIF_MLPL_WLPD          # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           EDGELOCK_HDSK_CTRL     # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           EDGELOCK_HDSK_STAT     # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           FUNC_STAT              # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           LPM_SETTING_0          # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           LPM_SETTING_1          # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           LPM_SETTING_2          # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           MLPL_CFG               # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           MLPL_STAT              # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           PSW_CNT_CFG_HF         # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           PSW_CNT_CFG_LF         # block not modelled; the catch-all answers 0
WAKEUP_MIX_SLICE           PSW_CTRL               # block not modelled; the catch-all answers 0
CCM                        CLOCK_ROOT0_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT10_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT11_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT12_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT13_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT14_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT15_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT16_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT17_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT18_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT19_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT1_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT20_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT21_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT22_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT23_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT24_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT25_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT26_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT27_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT28_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT29_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT2_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT30_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT31_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT32_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT33_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT34_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT35_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT36_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT37_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT38_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT39_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT3_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT40_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT41_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT42_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT43_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT44_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT45_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT46_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT47_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT48_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT49_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT4_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT50_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT51_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT52_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT53_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT54_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT55_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT56_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT57_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT58_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT59_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT5_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT60_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT61_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT62_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT63_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT64_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT65_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT66_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT67_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT68_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT69_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT6_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT70_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT71_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT72_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT73_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT7_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT8_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        CLOCK_ROOT9_AUTHEN     # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE0_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE0_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE0_AUTHEN_SET # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE0_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE1_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE1_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE1_AUTHEN_SET # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE1_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE2_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE2_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE2_AUTHEN_SET # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE2_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE3_AUTHEN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE3_AUTHEN_CLR # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE3_AUTHEN_SET # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        GPR_PRIVATE3_AUTHEN_TOG # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG0_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG0_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG0_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG0_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG100_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG100_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG100_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG100_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG101_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG101_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG101_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG101_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG102_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG102_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG102_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG102_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG103_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG103_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG103_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG103_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG104_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG104_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG104_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG104_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG105_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG105_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG105_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG105_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG106_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG106_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG106_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG106_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG107_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG107_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG107_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG107_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG108_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG108_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG108_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG108_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG109_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG109_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG109_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG109_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG10_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG10_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG10_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG10_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG110_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG110_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG110_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG110_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG111_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG111_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG111_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG111_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG112_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG112_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG112_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG112_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG113_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG113_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG113_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG113_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG114_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG114_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG114_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG114_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG115_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG115_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG115_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG115_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG116_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG116_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG116_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG116_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG117_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG117_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG117_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG117_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG118_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG118_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG118_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG118_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG119_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG119_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG119_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG119_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG11_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG11_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG11_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG11_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG120_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG120_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG120_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG120_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG121_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG121_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG121_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG121_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG122_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG122_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG122_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG122_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG123_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG123_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG123_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG123_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG124_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG124_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG124_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG124_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG125_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG125_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG125_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG125_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG126_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG126_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG126_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG126_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG127_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG127_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG127_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG127_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG128_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG128_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG128_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG128_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG129_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG129_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG129_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG129_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG12_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG12_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG12_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG12_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG130_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG130_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG130_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG130_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG131_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG131_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG131_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG131_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG132_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG132_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG132_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG132_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG133_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG133_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG133_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG133_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG134_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG134_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG134_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG134_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG135_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG135_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG135_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG135_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG136_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG136_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG136_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG136_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG137_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG137_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG137_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG137_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG138_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG138_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG138_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG138_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG139_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG139_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG139_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG139_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG13_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG13_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG13_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG13_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG140_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG140_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG140_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG140_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG141_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG141_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG141_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG141_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG142_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG142_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG142_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG142_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG143_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG143_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG143_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG143_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG144_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG144_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG144_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG144_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG145_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG145_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG145_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG145_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG146_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG146_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG146_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG146_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG147_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG147_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG147_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG147_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG148_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG148_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG148_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG148_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG14_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG14_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG14_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG14_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG15_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG15_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG15_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG15_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG16_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG16_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG16_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG16_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG17_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG17_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG17_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG17_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG18_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG18_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG18_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG18_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG19_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG19_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG19_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG19_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG1_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG1_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG1_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG1_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG20_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG20_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG20_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG20_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG21_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG21_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG21_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG21_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG22_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG22_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG22_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG22_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG23_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG23_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG23_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG23_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG24_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG24_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG24_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG24_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG25_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG25_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG25_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG25_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG26_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG26_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG26_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG26_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG27_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG27_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG27_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG27_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG28_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG28_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG28_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG28_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG29_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG29_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG29_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG29_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG2_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG2_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG2_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG2_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG30_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG30_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG30_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG30_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG31_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG31_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG31_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG31_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG32_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG32_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG32_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG32_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG33_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG33_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG33_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG33_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG34_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG34_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG34_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG34_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG35_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG35_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG35_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG35_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG36_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG36_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG36_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG36_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG37_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG37_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG37_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG37_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG38_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG38_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG38_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG38_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG39_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG39_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG39_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG39_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG3_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG3_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG3_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG3_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG40_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG40_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG40_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG40_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG41_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG41_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG41_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG41_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG42_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG42_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG42_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG42_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG43_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG43_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG43_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG43_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG44_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG44_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG44_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG44_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG45_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG45_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG45_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG45_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG46_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG46_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG46_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG46_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG47_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG47_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG47_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG47_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG48_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG48_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG48_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG48_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG49_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG49_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG49_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG49_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG4_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG4_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG4_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG4_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG50_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG50_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG50_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG50_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG51_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG51_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG51_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG51_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG52_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG52_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG52_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG52_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG53_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG53_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG53_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG53_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG54_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG54_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG54_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG54_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG55_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG55_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG55_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG55_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG56_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG56_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG56_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG56_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG57_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG57_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG57_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG57_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG58_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG58_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG58_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG58_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG59_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG59_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG59_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG59_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG5_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG5_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG5_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG5_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG60_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG60_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG60_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG60_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG61_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG61_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG61_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG61_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG62_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG62_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG62_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG62_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG63_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG63_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG63_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG63_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG64_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG64_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG64_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG64_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG65_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG65_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG65_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG65_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG66_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG66_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG66_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG66_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG67_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG67_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG67_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG67_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG68_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG68_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG68_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG68_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG69_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG69_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG69_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG69_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG6_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG6_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG6_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG6_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG70_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG70_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG70_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG70_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG71_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG71_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG71_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG71_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG72_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG72_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG72_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG72_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG73_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG73_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG73_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG73_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG74_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG74_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG74_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG74_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG75_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG75_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG75_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG75_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG76_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG76_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG76_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG76_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG77_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG77_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG77_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG77_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG78_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG78_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG78_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG78_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG79_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG79_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG79_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG79_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG7_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG7_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG7_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG7_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG80_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG80_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG80_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG80_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG81_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG81_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG81_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG81_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG82_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG82_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG82_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG82_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG83_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG83_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG83_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG83_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG84_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG84_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG84_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG84_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG85_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG85_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG85_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG85_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG86_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG86_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG86_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG86_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG87_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG87_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG87_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG87_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG88_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG88_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG88_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG88_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG89_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG89_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG89_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG89_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG8_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG8_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG8_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG8_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG90_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG90_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG90_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG90_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG91_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG91_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG91_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG91_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG92_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG92_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG92_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG92_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG93_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG93_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG93_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG93_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG94_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG94_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG94_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG94_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG95_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG95_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG95_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG95_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG96_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG96_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG96_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG96_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG97_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG97_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG97_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG97_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG98_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG98_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG98_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG98_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG99_AUTHEN          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG99_DIRECT          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG99_STATUS0         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG99_STATUS1         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG9_AUTHEN           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG9_DIRECT           # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG9_STATUS0          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        LPCG9_STATUS1          # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_AUTHEN_CLR    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_AUTHEN_SET    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_AUTHEN_TOG    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_FREQUENCY_CURRENT # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_FREQUENCY_MIN # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_HIGH_MIN      # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_LOW_MIN       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE0_PERIOD_MIN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_AUTHEN_CLR    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_AUTHEN_SET    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_AUTHEN_TOG    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_FREQUENCY_CURRENT # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_FREQUENCY_MIN # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_HIGH_MIN      # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_LOW_MIN       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OBSERVE1_PERIOD_MIN    # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL0_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL0_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL0_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL0_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL10_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL10_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL10_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL10_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL11_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL11_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL11_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL11_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL12_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL12_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL12_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL12_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL13_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL13_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL13_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL13_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL14_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL14_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL14_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL14_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL15_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL15_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL15_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL15_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL16_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL16_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL16_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL16_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL17_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL17_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL17_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL17_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL18_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL18_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL18_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL18_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL19_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL19_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL19_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL19_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL1_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL1_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL1_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL1_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL20_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL20_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL20_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL20_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL21_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL21_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL21_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL21_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL22_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL22_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL22_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL22_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL23_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL23_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL23_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL23_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL24_AUTHEN        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL24_DIRECT        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL24_STATUS0       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL24_STATUS1       # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL2_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL2_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL2_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL2_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL3_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL3_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL3_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL3_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL4_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL4_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL4_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL4_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL5_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL5_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL5_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL5_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL6_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL6_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL6_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL6_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL7_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL7_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL7_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL7_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL8_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL8_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL8_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL8_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL9_AUTHEN         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL9_DIRECT         # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL9_STATUS0        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
CCM                        OSCPLL9_STATUS1        # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (the gate could not see CCM's struct arrays until the extractor was fixed). The clock tree is next.
NETC_IERB                  SBCR                   # MODELLED BLOCK, reset value not set -- NEWLY VISIBLE (struct-array blind spot)
OTFAD1                     CTX0_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD1                     CTX1_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD1                     CTX2_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD1                     CTX3_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD2                     CTX0_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD2                     CTX1_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD2                     CTX2_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
OTFAD2                     CTX3_RGD_W1            # block not modelled (catch-all answers 0) -- NEWLY VISIBLE (struct-array blind spot)
USBNC_OTG1                 CTRL2                  # periphrdy stub, not a behavioural model -- NEWLY VISIBLE (struct-array blind spot)
USBNC_OTG2                 CTRL2                  # periphrdy stub, not a behavioural model -- NEWLY VISIBLE (struct-array blind spot)
